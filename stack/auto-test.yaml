apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: '{{ .name }}' 
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - git:
      repoURL: https://github.com/TejaswiniOpsmx/argo-customer-demo.git
      revision: new-test
      files:
        - path: "source-apps/config.json"
  template:
    metadata:
      name: '{{ .name }}'  # Directly reference the 'name' field for each application
    spec:
      project: default
      source:
        repoURL: "https://github.com/TejaswiniOpsmx/argo-customer-demo.git"
        targetRevision: "new-test"
        path: '{{ .path }}'  # Directly reference the 'path' field for each application
      destination:
        server: https://kubernetes.default.svc
        namespace: test
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
