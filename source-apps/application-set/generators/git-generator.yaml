# this generator i use for one git source and one helm source
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: git-application-set
  namespace: argocd
spec:
  generators:
    - git:                 
        repoURL: "https://github.com/TejaswiniOpsmx/argo-customer-demo.git"
        revision: "new-test"
        directories:
          - path: "source-data/application-set-data/git-data/*"
  template:
    metadata:
      name: '{{name}}'
      labels:
         for: jpmc
    spec:
      project: default
      source:
        repoURL: "https://github.com/TejaswiniOpsmx/argo-customer-demo.git"
        targetRevision: "new-test"
        path: '{{path}}'
      destination:
        server: "https://kubernetes.default.svc"
        namespace: 'default'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
